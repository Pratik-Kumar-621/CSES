#include <bits/stdc++.h>
using namespace std;

void solve(){
    cout<<"YES"<<endl;
}

int main(){
    string s;
    cin >> s;
    bool noSol = false
    int len = s.size();
    vector<char> ans;

    if()

    if(noSol){
        cout << "NO SOLUTION \n" ;
        return 0;
    }
    for(auto it:ans) cout << it;
    cout << endl;
}